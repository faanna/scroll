const sections = document.querySelectorAll('section');
let postionArray = [];

window.addEventListener('scroll', () => {
	// 스크롤은 무조건 윈도우에만 할수있음
	let scroll = window.scrollY;
	// 스크롤에도 칸수가 적용되있음
	// ->기본픽셀 100px : 한번움직이고 뒤로갈때마다 100px 증가/ 감소
});
